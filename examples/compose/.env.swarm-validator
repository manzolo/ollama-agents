# ============================================================================
# AGENT: swarm-validator
# ============================================================================
# Docker Swarm Validator Agent Configuration
# This file contains environment-specific configuration for the agent.
# ============================================================================

# Port to expose the agent on
PORT=7002

# Ollama model to use (optional - defaults to DEFAULT_MODEL from main .env)
# Uncomment to override the global default model
# MODEL_NAME=llama3.2

# Model temperature (optional - defaults to DEFAULT_TEMPERATURE from main .env)
# Uncomment to override: 0.0 = deterministic, 1.0 = creative
TEMPERATURE=0.1

# Maximum tokens to generate (optional - defaults to DEFAULT_MAX_TOKENS from main .env)
# Uncomment to override
# MAX_TOKENS=4096

# Ollama host URL (optional - defaults to OLLAMA_HOST from main .env)
# Uncomment to override for this specific agent
# OLLAMA_HOST=http://ollama:11434

# Agent name (used for logging and identification)
AGENT_NAME=swarm-validator
